# Ancient Heights Apartments

git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/aaron-san/ancient-heights.git
git push -u origin main


## Publish

1. Ctrl + Shft + P -> "Jupyter: Export to HTML"

2. Push to GitHub

```bash
git add .
git commit -m "message"
git push
```
